# DentalSync 프론트엔드 환경변수 — .env.local 로 복사 후 사용

# 백엔드 API 주소 (로컬 개발 기본값)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Phase 1 단일 베어러 토큰 — 미설정 시 dev 모드(인증 패스스루).
# 백엔드 API_AUTH_TOKEN 과 동일한 값이어야 한다.
NEXT_PUBLIC_API_AUTH_TOKEN=
